pipeline {
    agent any 
    stages {
        stage('PortTest') { 
            steps {
                echo "Making the 8283 port Test"
            }
        }
        stage('TokenTest') { 
            steps {
                echo "Making the Get Token Test"
            }
        }
    }
}
